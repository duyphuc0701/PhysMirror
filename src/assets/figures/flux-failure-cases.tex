\begin{figure}[tp]
    \centering
    % \vspace{3mm}
    \includegraphics[width=\linewidth]{figures/flux-failure-cases.pdf}
    % \vspace{-1mm}
    \caption{Typical failure cases of the FLUX.1-dev text-to-image model when generating mirror scenes. Common errors include (a) missing the reflection entirely, (b) missing the mirror itself, and (c) generating a mismatched object pose between the real object and its reflection.}
    \label{fig:flux-failure-cases}
\end{figure}